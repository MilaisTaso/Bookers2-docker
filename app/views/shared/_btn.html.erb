<% if book.user.id == current_user.id %>
  <td class="d-flex flex-column">
    <%= link_to 'edit', edit_book_path(book.id), class:"btn btn-success btn-sm mb-1" %>
    <%= link_to 'destroy', book_path(book.id), method: :delete, class:"btn btn-danger btn-sm", "data-confirm" => "本当に消去しますか?"%>
  </td>
<% end %>
